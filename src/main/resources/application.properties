spring.application.name=chartable
# spring.datasource.url=jdbc:mysql://localhost:3306/chartable_db
# spring.datasource.username=root
# spring.datasource.password=root

# Use Railway database URL (Remove local URL)
# spring.datasource.url=${MYSQL_URL}
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}

#dont touch this file 
server.port=8080
spring.datasource.platform=mysql

# Database Connection
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/podnova
spring.datasource.username=root
spring.datasource.password=Admin@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# SSH Tunnel Configuration (Handled externally)
# You are using an SSH tunnel, so you need to establish the SSH tunnel manually before running the application.

# Hibernate Properties
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf settings
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Static resources
spring.resources.static-locations=classpath:/static/

# # Set server port from Railway (Default: 8080 for local)
# server.port=${PORT:8080}
